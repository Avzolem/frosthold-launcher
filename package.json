{
  "name": "frosthold-launcher",
  "version": "0.1.1",
  "description": "Launcher de Frosthold",
  "main": "dist/main/index.js",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "manifest": "node scripts/build-manifest.mjs",
    "build": "tsc -p tsconfig.json",
    "dev": "yarn build && electron .",
    "dist": "yarn build && electron-builder --win nsis"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "electron": "^33.3.1",
    "electron-builder": "^25.1.8",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "electron-updater": "^6.3.9"
  }
}
